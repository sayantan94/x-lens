{
  "name": "x-lens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["ai", "agent", "tui", "app"],
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "clean": "npm run clean --workspaces --if-present",
    "build": "cd tui && npm run build && cd ../ai && npm run build && cd ../agent && npm run build",
    "dev": "npx concurrently -n tui,ai,agent \"cd tui && npm run dev\" \"cd ai && npm run dev\" \"cd agent && npm run dev\""
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.5",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.2",
    "tsx": "^4.20.3"
  }
}
